// auth.js - (Optional) AJAX helpers for login/register forms
// Example usage:
//
// const loginForm = document.getElementById('loginForm');
// if (loginForm) {
//   loginForm.addEventListener('submit', async e => {
//     e.preventDefault();
//     const formData = new FormData(loginForm);
//     const res = await fetch(`${BASE_URL}modules/auth/process_login.php`, {
//       method: 'POST',
//       body: formData
//     });
//     // handle JSON response or redirect manually
//   });
// }
